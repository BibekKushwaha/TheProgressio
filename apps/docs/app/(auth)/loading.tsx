import { PageLoader } from "@/components/layout/PageLoader";

export default function AuthLoading() {
    return <PageLoader title="Opening authentication" subtitle="Securing session and loading forms..." />;
}
