import { PageLoader } from "@/components/layout/PageLoader";

export default function DashboardLoading() {
    return <PageLoader title="Loading dashboard" subtitle="Pulling tasks, habits, and analytics..." />;
}
